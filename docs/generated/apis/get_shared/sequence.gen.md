# get_shared シーケンス

<!-- Generated by tools/design.py. Do not edit. -->

`GET /api/shared/{token}` / operationId: `get_shared`

ハンドラ: [backend/src/app/apis/notes/get_shared/router.py:11](https://github.com/tsuji-tomonori/CornellNoteWebv2/blob/dev/backend/src/app/apis/notes/get_shared/router.py#L11)

処理: [backend/src/app/apis/notes/get_shared/functions.py:12](https://github.com/tsuji-tomonori/CornellNoteWebv2/blob/dev/backend/src/app/apis/notes/get_shared/functions.py#L12)

routerから呼ぶ業務関数の分岐・transaction・例外をAST順に表示する。self呼出の外部ライブラリ内部、式中の短絡・内包表記内部は展開しない。breakはその経路の終了を表す。

```mermaid
sequenceDiagram
    participant C as 呼出元
    participant A as API / functions
    participant Q as 生成queries
    participant D as DB
    C->>A: execute
    A->>A: token.encode
    A->>A: hashlib.sha256
    A->>A: hashlib.sha256(token.encode()).hexdigest
    A->>A: datetime.now
    A->>A: queries.SelectSharedNoteParams
    rect rgb(240, 246, 244)
    Note over A: with repo.transaction()
    A->>Q: select_shared_note
    Q->>D: SELECT notes / 001_select_shared_note.sql
    D-->>Q: 実行結果（例外はtryの例外分岐へ）
    Q-->>A: 型付き結果
    Note over A: context終了（例外時も解放）
    end
    alt not rows
    break raise
    A-->>C: HTTPException(404, '共有リンクが無効または期限切れです')
    end
    end
    A->>A: decode
    break return
    A-->>C: decode(rows[0])
    end
```
