# get_note 詳細設計

<!-- Generated by tools/design.py. Do not edit. -->

`GET /api/notes/{note_id}` / operationId: `get_note`

ハンドラ: [backend/src/app/apis/notes/get_note/router.py:13](https://github.com/tsuji-tomonori/CornellNoteWebv2/blob/dev/backend/src/app/apis/notes/get_note/router.py#L13)

処理: [backend/src/app/apis/notes/get_note/functions.py:11](https://github.com/tsuji-tomonori/CornellNoteWebv2/blob/dev/backend/src/app/apis/notes/get_note/functions.py#L11)

所有者に一致するノートを返す。

## 入出力

```python
def execute(note_id: UUID, owner: str, repo: Database) -> Note:
```

## 条件分岐

| 行 | 条件式 |
| --- | --- |
| 15 | not rows |

## 呼出先と引数

| 行 | 関数 | 引数 |
| --- | --- | --- |
| 13 | repo.transaction |  |
| 14 | queries.SelectNoteParams |  |
| 14 | queries.select_note | session, queries.SelectNoteParams(id=note_id, owner_id=owner) |
| 16 | HTTPException | 404, 'ノートが見つかりません' |
| 17 | decode | rows[0] |

## 要件との直接対応

| 要件 | タイトル | 検証方法 |
| --- | --- | --- |
| REQ-OWNER | 他人のノートを保護する | automated-tests-and-review |

ファイル単位の正本traceのみ。未対応の要件を推測してAPIへ割り当てない。

## 処理本体（AST由来）

```python
def execute(note_id: UUID, owner: str, repo: Database) -> Note:
    """所有者に一致するノートを返す。"""
    with repo.transaction() as session:
        rows = queries.select_note(session, queries.SelectNoteParams(id=note_id, owner_id=owner))
    if not rows:
        raise HTTPException(404, 'ノートが見つかりません')
    return decode(rows[0])
```
