# update_note シーケンス

<!-- Generated by tools/design.py. Do not edit. -->

`PUT /api/notes/{note_id}` / operationId: `update_note`

ハンドラ: [backend/src/app/apis/notes/update_note/router.py:13](https://github.com/tsuji-tomonori/CornellNoteWebv2/blob/dev/backend/src/app/apis/notes/update_note/router.py#L13)

処理: [backend/src/app/apis/notes/update_note/functions.py:13](https://github.com/tsuji-tomonori/CornellNoteWebv2/blob/dev/backend/src/app/apis/notes/update_note/functions.py#L13)

routerから呼ぶ業務関数の分岐・transaction・例外をAST順に表示する。self呼出の外部ライブラリ内部、式中の短絡・内包表記内部は展開しない。breakはその経路の終了を表す。

```mermaid
sequenceDiagram
    participant C as 呼出元
    participant A as API / functions
    participant Q as 生成queries
    participant D as DB
    C->>A: execute
    A->>A: Depends(authenticate) / JWT検証
    Note over C,A: 認証エラーは401で終了（messages参照）
    Note over A: 条件評価・反復式 [t.model_dump(mode='json') for t in data.tasks]
    A->>A: json.dumps
    A->>A: datetime.now
    A->>A: queries.UpdateNoteParams
    critical try（通常経路）
    rect rgb(240, 246, 244)
    Note over A: with repo.transaction()
    A->>Q: update_note
    Q->>D: UPDATE notes / 001_update_note.sql
    D-->>Q: 実行結果（例外はtryの例外分岐へ）
    Q-->>A: 型付き結果
    Note over A: context終了（例外時も解放）
    end
    alt not rows
    rect rgb(240, 246, 244)
    Note over A: with repo.transaction()
    A->>A: queries.SelectOwnedNoteParams
    A->>Q: select_owned_note
    Q->>D: SELECT notes / 002_select_owned_note.sql
    D-->>Q: 実行結果（例外はtryの例外分岐へ）
    Q-->>A: 型付き結果
    Note over A: context終了（例外時も解放）
    end
    alt not owned
    break raise
    A-->>C: HTTPException(404, 'ノートが見つかりません')
    end
    end
    break raise
    A-->>C: HTTPException(409, '別の画面で更新されています。入力を控えて再読み込みしてください')
    end
    end
    A->>A: decode
    break return
    A-->>C: decode(rows[0])
    end
    option except UpdateConflict
    break raise
    A-->>C: HTTPException(409, '更新が競合しました。再読み込みしてください')
    end
    end
```
